#Domotiga Config
DOMOTIGA_BASE_URL = "http://localhost:9090"
ECHO_GROUP = "Alexa"

#Hue Bridge Config
HTTP_LISTEN_PORT = 8000		# Port the Hue Bridge will run on
PROXY = True              # Set to true whe running via proxy, false when standalone
PROXY_PORT = 80           # If running via proxy. What port the proxy is running on. Should be port 80 for Dot v3 and Google Home

